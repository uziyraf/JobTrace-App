name: jobtracker
description: "A new Flutter project for Job Tracking."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  lucide_icons: ^0.257.0
  fl_chart: ^0.66.0
  intl: ^0.20.2
  sqflite: ^2.4.2
  path: ^1.9.1
  flutter_local_notifications: ^20.1.0
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true